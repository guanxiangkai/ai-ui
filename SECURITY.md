# 安全策略

## 支持范围

安全修复只面向仍受维护的软件包最新稳定版本。基础包默认不持久化会话；产品显式启用浏览器存储、
跨窗口传输或自定义请求转换后，应自行完成 XSS、CSP、CSRF 和数据保留评估。

## 报告漏洞

请通过 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能提交问题，不要先创建
公开 Issue，也不要附带真实 Token、Cookie、个人信息、内部地址或客户数据。报告应尽量包含受影响
软件包、版本、最小复现步骤、影响判断和建议缓解措施。

维护者完成初步确认前，请不要公开可利用细节。若 GitHub 私密漏洞报告不可用，可先创建一个
不含利用细节和敏感数据的普通 Issue，请求维护者开启私密沟通渠道。

## 凭据泄露

若浏览器凭据可能已经泄露，应先撤销会话或轮换服务端凭据，再处理源码和发布制品；前端加密或删除
本地文件不能使已经暴露的令牌失效。
